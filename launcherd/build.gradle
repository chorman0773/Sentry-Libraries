
plugins {
    id 'java'
}

group 'github.chorman0773.sentry'
version '1.0'

dependencies {
    compileOnly project(":launch")
    runtimeOnly group: "github.chorman0773.sentry", name: "launch", version: "[1.0,2.0)"
}
